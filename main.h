#ifndef MAIN_H
#define MAIN_H

/**
 * struct op - struct up
 *
 * @op: The operator
 * @f: The function associated
 */
typeof struct conversion_specifier
{
	char *csp;
	int (*f)(va_list);
} conv_s;
int _printf(const char *format, ...);
#endif
